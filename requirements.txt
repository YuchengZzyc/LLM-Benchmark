# =============================================================================
# Benchmark Framework — 运行时依赖
# 说明：
#   1. 当前阶段为框架搭建，仅声明依赖，不执行任何安装。
#   2. 请勿直接 pip install -r requirements.txt（可能缺少 GPU 版 torch）。
#      推荐按 docs/environment_setup.md 的步骤在服务器环境安装。
#   3. 各依赖的版本范围为主框架 lm-eval 的常见兼容区间，
#      安装时建议查阅对应版本的官方 requirements。
# =============================================================================

# ---- 主框架：通用能力评测 ----
lm-eval>=0.4.2,<0.7.0

# ---- 推理后端（GPU 版请单独安装，见 environment_setup.md）----
# torch>=2.1.0
# transformers>=4.40.0
# accelerate>=0.29.0

# ---- 数据与计算 ----
numpy>=1.24.0
datasets>=2.18.0

# ---- LongBench 长上下文独立接口（src/longbench.py）----
# 评分依赖：ROUGE-L（摘要任务）由 rouge-score 提供。
rouge-score>=0.1.2

# ---- 配置解析 ----
PyYAML>=6.0.0

# ---- 基础工具 ----
tqdm>=4.66.0
